/*
function hi(name, age) {
    console.log(`Hi ${name}, you are ${age}`);
}

hi("Chris", 24);
*/

concatFirstLast('Chris', 'Werling');

function concatFirstLast (first, last) {
    let fullName = first + ' ' + last;
    console.log(`Hello, my name is ${fullName}.`);
}